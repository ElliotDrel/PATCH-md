<!-- Installer pastes this into CLAUDE.md / AGENTS.md; replaces <UPSTREAM>. -->
## PatchMD

This fork tracks `<UPSTREAM>`. When you deliberately change upstream-owned code,
record the intent in root `PATCH.md` (run `modify-with-patch-md`). Before pulling
upstream, run `update-with-patch-md`.
